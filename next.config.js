/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: "/new-next-app",
  basePath: "/new-next-app",
  trailingSlash: true,
  reactStrictMode: true,
}

module.exports = nextConfig
